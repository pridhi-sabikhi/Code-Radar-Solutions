#include <stdio.h>
int main() {
    int N;
    scanf("%d", &N);
    for(int i=1; i<=N; i++)
    {
        printf("%d ",i); //we want to print every no. with space so wrote like this in printf
    }
    return 0;
}