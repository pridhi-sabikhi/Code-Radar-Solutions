//see cp 29 jan wala usmein found max using if, switch, ternary operator
#include <stdio.h>
int main() {
    int num1, num2, num3;
    scanf("%d %d %d", &num1, &num2, &num3);
    if(a>b && a>c)
    printf("%d",a);
    else if(b>c && b>a)
    printf("%d", b);
    else
    printf("%d", c);
        return 0;
}