#include <stdio.h>
//#include <string.h>// to use string functs
int main() {
    int num;
    string str;
    scanf("%d", &num);
    while(n>0)
    {
        int r=n%2;
        s=(string)r+s;
        n=n/10;
    }
    printf("%s", str);
    return 0;
}