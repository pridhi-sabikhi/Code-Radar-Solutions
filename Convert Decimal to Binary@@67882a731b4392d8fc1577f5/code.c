#include <stdio.h>
//#include <string.h>// to use string functs
int main() {
    int num, r;
    string str, s;
    scanf("%d", &num);
    while(n>0)
    {
        r=n%2;
        s=(string)r;
        str=s+str;
        n=n/2;
    }
    printf("%s", str);
    return 0;
}