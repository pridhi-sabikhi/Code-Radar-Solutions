#include <stdio.h>
//#include <string.h>// to use string functs
int main() {
    int num, r;
    string str;
    scanf("%d", &num);
    while(n>0)
    {
        r=n%2;
        str=(string)r+str;
        n=n/2;
    }
    printf("%s", str);
    return 0;
}