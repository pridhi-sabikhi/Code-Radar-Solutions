#include <stdio.h>
#include <string.h>
int main()
{
    char s[100];
    int i=0, count =0;
    scanf("%c", s);
    while(isalpha(sent[i]))
    {
        count++;
    }
    printf("%d", count);
    return 0;
}