#include <stdio.h>
int main() {
    int s1, s2, s3;
    scanf("%d %d %d", &s1, &s2, &s3);
    if ((a+b)>c && (b+c)>a &&(c+a)>b){
        printf("Valid");
    }
    else{
        printf("Inavlid");
    }
    return 0;
}